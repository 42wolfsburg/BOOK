services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - API_URL=${API_URL:-http://backend:3000}
    command: ["npm","run","dev","--","--host","0.0.0.0"]
